name: dmx_openapi_example
description: A dmx macro written in Dart that generates an API client from an OpenAPI document, laid out by Mustache templates.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.0.0

dependencies:
  dmx:
    path: ../../src/dart_packages/dmx

dev_dependencies:
  test: ^1.25.0
